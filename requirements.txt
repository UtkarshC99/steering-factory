torch>=2.1
transformers>=5.0   # v4.x chokes on newer tokenizer_config.json formats (extra_special_tokens as list)
huggingface_hub>=0.24   # login() for gated models (e.g. google/gemma-3-4b-it), used directly by the Colab notebook
accelerate>=0.33
bitsandbytes>=0.43; platform_system != "Darwin"
sentencepiece
numpy
scipy
pyyaml
streamlit>=1.36
pandas
plotly
ipywidgets>=8.0   # required for plotly FigureWidget live updates in live_plot.py (notebook-only)
matplotlib>=3.8   # static PNGs for comparison_plots.py (report.md images); comparison.py/comparison_report.py stay plot-free without it
anthropic
openai
pytest
datasets>=2.19
packaging   # version check in datasets.py's AbstentionBenchAdapter (needs `datasets` pinned <=3.6.0 separately, see its docstring)
jsonschema>=4.22
psutil
pyarrow>=15
# Optional Colab comparison arm (install with: pip install peft trl)
peft>=0.11
trl>=0.9
